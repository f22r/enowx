import { AppShell, Empty } from "./shell";

export function SettingsApp() {
  return (
    <AppShell title="Settings" subtitle="Port, config, sync">
      <Empty message="Settings coming soon." />
    </AppShell>
  );
}
